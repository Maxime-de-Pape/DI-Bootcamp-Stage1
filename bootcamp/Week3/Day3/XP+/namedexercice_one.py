import func

# Get the numbers from the user.
a = int(input("Enter the first number: "))
b = int(input("Enter the second number: "))

# Add the numbers and print the result.
sum = func.add_numbers(a, b)
print(f"The sum of {a} and {b} is {sum}")
